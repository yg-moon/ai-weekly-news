# 작업 체크리스트

지금 무엇을 하는 중인지 담는다. 끝난 항목은 지운다. 왜 만드는지는 [`docs/INTENT.md`](docs/INTENT.md), 매주 어떻게 만드는지는 [`docs/ROUTINE.md`](docs/ROUTINE.md) 에 있다.

## 다음 세션 시작 순서

- 1순위: 2026-W36 초안에 대한 사용자 검수 결과를 콘텐츠와 `docs/ROUTINE.md` 선별 기준에 반영한다.
- 2순위: 승인되면 `docs/ROUTINE.md` 의 설정으로 트리거를 등록하고 첫 자동 실행(대상 2026-W37)을 지켜본다.

## Phase 1 — 첫 주간호 (초안 발행됨)

- [x] 2026-W36 수동 수집·작성 (국내 4 · 해외 5 · AI 5)
- [x] 빌드 스크립트와 Actions 빌드 배포
- [ ] **사용자 검수**: https://yg-moon.github.io/ai-weekly-news/week/2026-W36/ — 서술·분량·디자인·모바일
- [ ] 검수 결과 반영

## Phase 2 — 자동화

- [ ] 사용자 승인 후 `create_trigger` 로 트리거 등록 (설정값은 ROUTINE.md)
- [ ] 새 세션에서 `add_repo` 로 레포 접근이 되는지 — 첫 자동 실행에서 검증
- [ ] 푸시 알림이 실제로 도착하는지 — 첫 자동 실행에서 검증
- [ ] (선택) 환경 네트워크 정책을 열어 `WebFetch` 본문 읽기가 되는지 실험

## 나중에 결정할 것

- 월간 인사이트를 어떤 방식으로 생성할지
- 분기·반기 단위를 둘지
- 라이선스 — 코드와 콘텐츠를 나눌지

## 해보고 알게 된 것

첫 주간호를 만들며 확인한 것이다. 영구 문서로 올라간 것은 여기서 지운다.

### 수집 — `docs/ROUTINE.md` 2·3절에 반영됨

- 국내 5건을 못 채웠다. 국문 주요지가 검색 크롤러를 차단해 영문판 6개 매체로 좁혀졌고, 5번째 후보 둘(미 반도체 관세 협상, 호르무즈 파병 검토)은 서울경제 영문판 단독이었다.
- 해외·AI는 5건씩 채웠다. 접근 가능한 매체 7~8개로 충분했다.
- 검색 요약의 수치를 그대로 믿으면 안 된다. 8월 수출 68.7% 는 처음엔 비현실적으로 보였으나 세 매체가 일치해 실었다. 반대로 요약이 만들어 낸 문장이 어느 매체에도 없는 경우도 있었다.
- 검색 한 번에 15건을 뽑는 방식은 저품질 블로그와 Substack 이 섞인다. 매체 도메인을 제한하면 품질이 확연히 다르다.

### 환경

- 세션에서 저장소 생성 불가, Pages 활성화 불가(REST API 는 프록시 차단, Actions `GITHUB_TOKEN` 은 권한 없음), `github.io` 아웃바운드 차단. 전부 사용자가 처리했거나 우회했다.
- 네트워크 egress 는 GitHub·npm·pypi 외 전부 차단이다. 뉴스 사이트 본문은 어느 곳도 못 읽는다. Claude Code 웹의 환경 설정에서 네트워크 정책을 바꾸면 달라질 수 있다.
- GNU date 에 `'2026-09-13 23:21 - 7 days'` 처럼 시각 뒤에 `-` 를 쓰면 시간대 오프셋으로 오독한다. 상대 표현은 `'last sunday'` 처럼 쓰거나 날짜만 넘긴다.
