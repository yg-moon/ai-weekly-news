# 작업 체크리스트

지금 무엇을 하는 중인지만 담는다. 끝난 항목은 지운다. 왜 만드는지는 [`docs/INTENT.md`](docs/INTENT.md), 매주 어떻게 만드는지는 [`docs/ROUTINE.md`](docs/ROUTINE.md) 에 있다.

## 다음 세션 시작 순서

- 1순위: Pages가 실제로 뜨는지 확인한다.
- 2순위: 지난 주 1개 주차를 수동으로 만들어 보고 수집 가능한 깊이를 파악한다.
- 3순위: 그 결과로 빌드 방식과 트리거를 확정한다.

## Phase 0 — 발행 경로 뚫기

- [x] 레포 생성과 문서 골격
- [ ] **사용자 작업**: Settings → Pages → Source를 `GitHub Actions` 로 변경
- [ ] Actions 워크플로가 성공하고 사이트가 실제로 열리는지 확인
- [ ] 모바일 폭에서 레이아웃이 깨지지 않는지 확인

## Phase 1 — 한 주 실제로 만들어 보기

- [ ] 지난 주 1개 주차를 수동으로 수집·작성해 본다
- [ ] 세 분야 각각 5건을 기준을 지키며 채울 수 있는지 확인한다
- [ ] 못 채우는 분야가 있다면 무엇이 병목인지 기록한다
- [ ] 실험 결과를 `docs/ROUTINE.md` 의 선별 기준에 반영한다

## Phase 2 — 자동화

- [ ] `content/` 마크다운에서 `site/` HTML을 만드는 빌드 단계를 둘지 결정한다
- [ ] `create_trigger` 로 주간 트리거를 등록한다 (설정값은 ROUTINE.md)
- [ ] 새 세션에서 `add_repo` 로 레포 접근이 되는지 검증한다
- [ ] 푸시 알림이 실제로 도착하는지 검증한다

## 나중에 결정할 것

- [ ] 월간 인사이트를 어떤 방식으로 생성할지
- [ ] 분기·반기 단위를 둘지
- [ ] 라이선스 — 코드와 콘텐츠를 나눌지
