# 작업 체크리스트

지금 무엇을 하는 중인지 담는다. 끝난 항목은 지운다. 왜 만드는지는 [`docs/INTENT.md`](docs/INTENT.md), 매주 어떻게 만드는지는 [`docs/RUNBOOK.md`](docs/RUNBOOK.md) 에 있다.

## 다음 세션 시작 순서

- 1순위: 2026-W36 개정판에 대한 사용자 검수 결과를 반영한다.
- 2순위: 승인되면 `docs/RUNBOOK.md` 의 설정으로 스케줄러를 등록하고 첫 자동 실행(대상 2026-W37)을 지켜본다.

## Phase 1 — 첫 주간호

- [x] 2026-W36 수집·작성 (국내 5 · 해외 5 · AI 5)
- [x] 빌드 스크립트와 Actions 빌드 배포
- [x] 원문 대조 절차 확립 — 네트워크 정책 완화 후 `scripts/read-article.mjs` 로 본문·발행일 확인
- [ ] **사용자 검수**: https://yg-moon.github.io/ai-weekly-news/week/2026-W36/ — 서술·분량·디자인·모바일

## Phase 2 — 자동화

- [ ] 사용자 승인 후 스케줄러 등록 (설정값은 RUNBOOK.md "플랫폼별 실행")
- [ ] 새 세션에서 저장소 접근이 되는지 — 첫 자동 실행에서 검증
- [ ] 푸시 알림이 실제로 도착하는지 — 첫 자동 실행에서 검증

## 장기 과제 — 내용 정확성 검증

`docs/INTENT.md` 의 "장기 과제" 참고. 사람이 개별 항목을 검증하지 않는다는 사실을 사이트에 밝혔고, 다음은 그 한계를 좁히는 일이다.

- [ ] 본문의 수치를 출처 원문에서 다시 찾아 대조하는 자동 검사
- [ ] 링크 생존과 발행일 범위 검사를 빌드에 넣기
- [ ] 복수 출처 간 수치 불일치 탐지
- [ ] 독자가 오류를 알릴 경로와 정정 이력 남기기

## 나중에 결정할 것

- 월간 인사이트를 어떤 방식으로 생성할지
- 분기·반기 단위를 둘지
- 라이선스 — 코드와 콘텐츠를 나눌지

## 해보고 알게 된 것

여기서 확인된 것 중 매주 필요한 것은 `docs/RUNBOOK.md` 로 올라갔다. 이 절은 아직 안 올라간 것과 환경 특이사항만 남긴다.

### 네트워크 — 정책을 "전체"로 바꾼 뒤

- `curl`·Node 로는 뉴스 사이트 원문을 받을 수 있다. 발행일 메타태그까지 읽힌다. 이것이 원문 대조 절차의 근거다.
- `WebFetch` 도구는 여전히 `EGRESS_BLOCKED` 를 반환한다. 도구 자체의 별도 차단 목록으로 보인다. 원문 읽기는 `scripts/read-article.mjs` 로 한다.
- 검색 도구의 `allowed_domains` 는 크롤러 차단 매체(Reuters·AP·BBC·NYT·조선·중앙·연합 등)를 넣으면 호출 전체가 실패한다. 목록에 넣지 않는다. 다만 URL 을 알면 원문 받기는 된다.

### 이전 세션의 기록 오류 (정정됨)

- 이전 RUNBOOK 에 "검색 요약은 소형 모델이 합성한 것"이라고 적었으나 근거 없는 서술이었다. `WebFetch` 도구 설명의 문구를 검색 도구에 잘못 갖다 붙인 것이다. 하위 에이전트는 이 프로젝트에서 한 번도 쓰이지 않았다.
- 요약을 의심해야 하는 실제 이유는 "원문과 대조되지 않은 생성 텍스트"라는 점이다. RUNBOOK 2절이 그렇게 고쳐졌다.

### 환경

- 세션에서 저장소 생성 불가, Pages 활성화 불가(REST API 는 프록시 차단, Actions `GITHUB_TOKEN` 은 권한 없음), `github.io` 아웃바운드 차단. 전부 사용자가 처리했거나 우회했다.
- 실행 환경은 UTC 이며 회수되면 사라진다. 남길 것은 반드시 커밋한다.
