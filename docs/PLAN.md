# 작업 체크리스트

**지금 무엇을 하는 중인가.** 진행 중인 일과 아직 결론이 안 난 것을 담는다. 끝난 항목은 지운다.

## 다음 세션 시작 순서

**데드라인을 기준으로 삼지 않는다.** 정기 실행은 이미 끝까지 도는 것을 확인했고, 남은 문제는 무엇을 어떤 형식으로 내느냐다. 9월 28일에 분기호가 못 나와도 된다.

사용자가 정한 순서다. 앞의 것이 끝나야 다음으로 간다.

1. **문서를 읽고 개선한다.** 사용자가 `docs/` 를 정독하고 수정 제안을 낸다. 그것을 반영한다.
2. **그때 쓴 기준을 검사로 남긴다.** 사용자가 무엇을 보고 고치자고 했는지가 곧 기준이다. 그 기준이 다음에도 지켜지도록 `QUALITY_CHECKS.md` 에 항목으로 넣는다. 한 번 읽고 끝내면 같은 것이 다시 어긋난다.
3. **분야 도입 문단 규칙을 고친다.** 아래 점검에서 나온 것이다. 백필 전에 해야 한다.
4. **3분기를 백필한다.** W27~W34 여덟 주차다.
5. **분기호를 발행한다.** 2026-Q3 이며, 백필이 끝난 온전한 13주가 입력이다.

- 네 주차만으로 분기호를 시험 발행하는 방안은 접었다. 백필한 13주로 한 번에 제대로 낸다.
- 연간호는 한참 뒤의 일이다. 지금 손대지 않는다.
- 기간 탭 레이아웃은 백필과 의존성이 없어서 순서 밖에서 먼저 끝냈다.

### 백필 전 점검에서 나온 것 (2026-09-22)

W35~W38 네 주차를 `QUALITY_CHECKS.md` 3절 전부와 독자 읽기로 점검했다. 기계 검사는 깨끗했는데 세 가지가 남았고, 앞의 하나만 백필을 막는다.

- **분야 도입 문단의 기준이 흔들린다.** W37·W38 은 사건 이름부터 쓰는데 W35 셋은 "다섯 건의 성격이 서로 다르다", "앞의 둘은 … 뒤의 셋은 …" 처럼 편집 구성을 설명한다. 사건 이름이 하나도 없어 독자가 읽고도 무슨 일이 있었는지 모른다. 원인은 `RUNBOOK_WEEKLY.md` 12절이 "공통점이 없으면 성격이 서로 다르다고 쓴다"고 허용하는 것이다. 주당 3개라 백필하면 24개가 더 생긴다.
- [x] **매체 문제는 고쳤다.** 7절 표에 없던 NBC News 가 발표 주체로 분류되어 주요 매체 없는 항목이 지나갔다. 파고 들어가 보니 W36 이 막힌 매체를 14번 인용하고 있었다. 막힌 링크를 빼고 받아지는 원문으로 다시 대조해 거기 없는 사실을 뺐고, 주요 매체가 없던 두 항목에 주요 매체를 찾아 넣었다. 출처 검사가 이제 주요 매체 없는 항목과 막힌 매체를 함께 짚는다.
- **3.3.2 가 놓치는 유형이 실제로 있다.** W36 국내 도입의 "가장 크게 다뤄졌다"와 AI 도입의 "이 주 최대 사안이다" 가 낱말 목록에 없어 안 걸린다. 문서가 경고한 바로 그 유형이다.

뒤의 둘은 항목 수와 무관하게 고칠 수 있어 백필과 같이 가도 된다.

## Phase 1 — 첫 주간호

- [x] 2026-W36 수집·작성 (국내 5 · 해외 5 · AI 5)
- [x] 빌드 스크립트와 Actions 빌드 배포
- [x] 원문 대조 절차 확립 — `scripts/read-article.mjs` 로 본문·발행일 확인
- [x] 2026-W37 수집·작성 — 수동 실행으로 절차 전체를 한 번 돌렸다
- [x] 2026-W35 발행 — 스케줄러가 띄운 세션이 시험 경로로 집필한 것을 받아 발행했다
- [x] 발행물 검수 — 출처 표기와 순서에 대한 지적을 반영했다
- [x] **문서 검수 1차**: 사용자가 사이트 문구와 `AGENTS.md` 에 대한 수정 제안을 냈고 반영했다

## Phase 2 — 자동화

- [x] 스케줄러 등록 — `trig_01KwmsfoiEoNK66jH3XRyyFn`, 첫 실행 2026-09-21(월) 08:03 KST
- [x] 새 세션에서 저장소 읽기 — `git clone` 이 익명으로 한 번에 된다
- [x] 1절 가드 동작 — 대상 주가 이미 발행되어 있으면 수집으로 넘어가지 않고 종료한다
- [x] 저장소를 루틴에 등록한다 — 등록됐다. 발화된 세션이 자격 증명을 받아 푸시한다
- [x] `main` 에 직접 푸시가 통과하는지 — 통과한다. 2026-09-17 확인
- [x] 푸시 알림이 실제로 도착하는지 — 2026-09-21 첫 정기 실행에서 도착했다
- [x] 새 세션이 런북만으로 같은 품질을 내는지 — W35 로 시험 경로를 끝까지 돌렸다. 15건 선정과 집필을 마쳤고 QUALITY_CHECKS 3절을 첫 시도에 전부 통과했다
- [x] 정기 실행이 `main` 발행까지 끝내는지 — 2026-09-21 08:03 KST 발화가 2026-W38 을 발행했다. 08:22 KST 커밋, 19분

## 사용자가 직접 해야 하는 것

- [ ] **저장소 문서를 마저 읽고 다듬는다.** `AGENTS.md` 는 한 번 돌았다. `docs/` 나머지가 남았다
- [x] 저장소 About 에 Description 과 Website 를 넣는다 — 들어가 있다. Description 은 사이트 태그라인·README 첫 문장과 같다
- [ ] **루틴 모델을 정한다.** 2026-09-23 09:17 KST 에 루틴이 수정되어 모델 칸이 비었다. 비어 있으면 기본 모델로 돈다. 주간 런북 플랫폼 절의 표는 `claude-opus-5` 라고 적고 있다

## 분기호와 연간호

빈도와 경계, 실행 방식, 내용 규칙까지 정했다. 정한 것은 `INTENT.md` 와 `DECISIONS.md` 에, 절차는 `RUNBOOK_QUARTERLY.md` 에 있다.

2026-W39 발행일인 2026년 9월 28일이 첫 분기말이지만 **그날 맞춰 내지 않는다.** 형식이 자리잡는 것이 먼저다.

- [x] 분기호에 무엇을 싣고 어떤 형식으로 쓸지 정해 `RUNBOOK_QUARTERLY.md` 에 더한다 — 흐름 5 + 단발 5 고정
- [x] 빌드 스크립트가 `content/quarter/` 를 읽고 별도 페이지로 내게 한다 — 기간 목록 맨 위에 놓인다
- [ ] W27~W34 백필. 온전한 분기호를 내려면 결국 필요하다
- [ ] 반기 단위를 둘지는 분기를 여러 번 돌려 보고 정한다
- [ ] 연간호는 네 분기가 쌓인 뒤의 일이다. 지금 손대지 않는다

## 백필 — 과거 주차를 어디까지 만들 수 있는가

수집원이 전부 날짜로 받는 것이라 과거 주차도 지정해서 돌릴 수 있다.

**목표는 2026년 3분기를 온전히 채우는 것이다.** 3분기는 W27~W39 이며 W27 이 6월 29일에 시작해 7월 1일을 품는다. 7월 뉴스부터다.

- [ ] W27~W34 (8주) — 백필해야 하는 전부다. 한 주에 약 $18 이므로 약 $144 다
- W35·W36·W37 은 수동으로, W38 은 정기 실행이 발행했다. W39 는 9월 28일 정기 실행이 채운다

**9월 28일에 W39 가 발행되어도 2026-Q3 분기호는 돌지 않는다.** 분기 런북 1절에 멈추는 줄을 넣어 두었다. 그래서 백필이 늦어도 된다. 온전한 분기호를 내려면 결국 필요하고, 7일 사용 한도가 있어 나눠 실행한다.

1분기와 2분기는 범위 밖이다. 3분기 결과를 보고 나중에 정한다. W1 은 AI 1차 발표가 1건뿐인 주차라 1분기의 난이도를 미리 보여 준다.

잰 것.

- [x] **네이버 랭킹 아카이브의 끝은 2020년 11월 중순이다.** 2020-11-15 는 밖이고 2020-11-22 는 받아진다. 그보다 앞선 날짜는 오늘 랭킹이 대신 오며, 가드를 넣기 전에는 그것을 그대로 썼다
- [x] **2026년 전 구간에 수집 경계가 없다.** W1·W3·W5·W9·W17·W35 를 이분탐색으로 재 보니 국내는 어디서나 525건/7일이고 위키도 7일 모두 있다
- [x] **원문 생존도 온전하다.** 국내 기사를 W1·W17·W35 에서 12건씩 받아 모두 성공했다
- [x] **주차별 AI 건수는 그 주의 발표량을 따른다.** W1 이 1건, W5 가 24건, W35 가 26건이다. W1 은 연말연시라 발표가 없었다
- [x] **W35 를 스케줄러 세션이 집필해 발행했다.** 3주 전 주차로 15건을 채웠다

남은 것.

- [ ] 해외 원문 생존율 — 위키 Current events 가 출처를 매체 이름으로만 주고 URL 을 안 줘서 표본을 못 뽑았다. 별도 방법이 필요하다
- [ ] 후행 지식이 새는지 본다. 발행일 필터로 막을 수 있는 부분과 막을 수 없는 부분을 가른다

## 장기 과제 — 내용 정확성 검증

사람이 개별 항목을 검증하지 않는다는 사실은 사이트에 밝혔다. 다음은 그 한계를 좁히는 일이다.

- [ ] 본문의 수치를 출처 원문에서 다시 찾아 대조하는 자동 검사
- [ ] 링크 생존과 발행일 범위 검사를 빌드에 넣기
- [ ] 복수 출처 간 수치 불일치 탐지
- [ ] 독자가 오류를 알릴 경로와 정정 이력 남기기

## 해보고 알게 된 것

- 빈도 도구는 입력의 성격을 탄다. `rank-topics.mjs` 를 주간호 제목에 돌리면 상위 토큰이 `공개·미국·장관` 같은 일반어로 나온다. 날것의 중복 많은 헤드라인 525건에 맞춰 만든 것이라, 이미 선별을 거친 195건에는 선별용으로 안 맞는다. 도구를 다른 데 쓰기 전에 그 입력으로 한 번 돌려 본다.
- 수집 풀에 없다고 보도가 없는 것은 아니다. 물가 기사가 네이버 많이 본 랭킹에 없어 보조 매체만으로 실었는데, 언론사별 날짜 목록에는 주요 매체 다섯 곳에 있었다. 찾는 방법은 `RUNBOOK_WEEKLY.md` 8절에 있다.
- 읽기는 프록시가 익명으로 주지만 푸시는 루틴에 저장소가 등록되어 있어야 한다. 읽기와 쓰기가 여기서 갈린다.
- 수동 발화에 딸려 보낸 지시는 세션이 실행하지 않는다. 페이로드는 데이터로 취급된다. 세션에 시킬 일은 런북이나 프롬프트에 적어야 한다.
- 같은 사실을 두고 세션마다 보고가 엇갈릴 수 있다. GitHub MCP 가 있느냐를 두고 한 세션은 없다고, 다른 세션은 쓰고 있다고 했다. 세션의 자기 보고는 근거 하나일 뿐이다.
- 냉시작 세션이 확인할 수 없는 과거를 추측으로 메웠다. 첫 발화에서 "정기 실행이 월요일에 이미 W37 을 발행한 뒤"라고 적었는데 W37 은 수동 발행이었다. 결론은 맞았고 근거가 틀렸다.
- 소스가 범위 밖 요청에 오류 대신 엉뚱한 데이터를 주기도 한다. 네이버는 아카이브 밖 날짜에 오늘 랭킹을 돌려줬다. 받아진 것이 요청한 것인지 확인하는 장치가 있어야 한다.
- 검사를 만들면 문서의 빈틈이 드러난다. 출처 순서 검사가 7절 매체 표에 Axios 와 Gulf News 가 빠져 있던 것을 찾아냈다. 규칙만 두고 검사를 안 만들면 이런 것이 조용히 쌓인다.
- 네이버 랭킹은 EUC-KR 이고 Node 의 `fetch` 는 403 을 받아 `curl` 로 받는다.
- 실행 환경은 UTC 이며 회수되면 사라진다. 남길 것은 반드시 커밋한다.
- 세션에서 저장소 생성과 Pages 활성화는 불가능하다. 사용자가 직접 처리했다.
