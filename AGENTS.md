# AGENTS.md

이 저장소에서 작업하는 AI 에이전트가 세션마다 읽는 파일이다. 컨텍스트 비용이 들므로 **짧게 유지한다**. 상세는 아래 문서로 넘긴다.

## 이 저장소는 무엇인가

지난 한 주의 뉴스를 국내 5건·해외 5건·AI 5건으로 정리해 매주 월요일 오전(KST) GitHub Pages로 발행한다.

## 에이전트 중립 제약

이 프로젝트는 특정 AI 제품에 종속되지 않는다. 어떤 에이전트가 이어받아도, 사람이 수동으로 돌려도 같은 결과가 나와야 한다.

- 절차는 **도구 이름이 아니라 할 일**로 쓴다. "WebSearch 로 검색한다"가 아니라 "웹 검색으로 후보를 찾는다"로 쓴다.
- 특정 플랫폼에서만 되는 것은 본문에 섞지 않고 `docs/RUNBOOK.md` 의 "플랫폼별 실행" 절로 분리한다.
- 주기 실행은 크론 표현식으로 정의한다. 크론을 지원하는 플랫폼이면 어디서든 같은 스케줄로 돌 수 있어야 한다.
- 빌드는 Node 스크립트 하나로 끝난다. 특정 서비스의 빌드 기능에 기대지 않는다.

## 모델 사용 규칙

- **하위 에이전트에 위임하지 않는다.** 수집과 선별은 메인 세션이 직접 한다. 작은 모델에 넘기면 출처 대조가 느슨해진다.
- 부득이하게 위임한다면 **메인 세션과 같은 모델을 명시적으로 지정한다**. 기본값에 맡기지 않는다.
- 검색 도구가 돌려주는 산문 요약은 원문이 아니다. 사실로 쓰려면 원문을 직접 받아 대조한다.

## 작업 전에 읽을 것

- 주간 발행 작업이면 [`docs/RUNBOOK.md`](docs/RUNBOOK.md) 를 처음부터 끝까지 읽고 그대로 따른다.
- 범위·품질·목표에 대한 판단이 필요하면 [`docs/INTENT.md`](docs/INTENT.md) 를 따른다.
- 지금 무엇을 하는 중인지는 [`PLAN.md`](PLAN.md) 에 있다.

## 작업 규칙

- 확정 전에 실험한다. 되는지 확인하지 않은 것을 된다고 쓰지 않는다.
- 바꾸기 전에 논의한다. 선택지와 근거를 먼저 내놓고 결정은 사용자가 한다.
- 근거는 선례에서 찾는다. 자기 판단보다 기성 관행을 확인한 결과를 우선한다.
- 억지 부리지 않는다. 안 되는 건 안 된다고 말하고 한계를 그대로 밝힌다.
- 발행된 주간호의 본문은 덮어쓰지 않는다. 오류는 파일 끝에 정정을 덧붙인다.

## 문서 작성 규칙

- 가독성: 의미 단위의 subsection으로 나누고 포인트당 하나의 bullet을 쓴다.
- 간결성: 중복 표현을 피하고 하나의 의미는 하나의 문장으로 압축한다.
- 최소 변경: 내용을 추가할 때 기존 문서의 맥락을 고려해 최소한으로 바꾼다.
- 명확성 우선: 짧게 줄여서 뜻이 갈릴 수 있으면 길더라도 명확한 쪽을 쓴다. 기간·단위·날짜 표기가 특히 그렇다.

## 문서의 역할 분담

| 문서 | 답하는 질문 | 수명 |
|---|---|---|
| `AGENTS.md` | 이 저장소에서 일하는 **모든** 에이전트가 알아야 할 것 | 영구 · 최소 |
| `docs/INTENT.md` | 왜 만드는가, 무엇이 확정됐는가 | 영구 |
| `docs/RUNBOOK.md` | 매주 어떻게 실행하는가 | 영구 · 자주 다듬음 |
| `PLAN.md` | 지금 무엇을 하는 중인가, 무엇을 해보고 무엇을 알았는가 | 휘발성 · 길어도 됨 |

- 영구 문서 셋은 짧게 유지하고 상황판인 `PLAN.md` 만 길어지는 것을 허용한다.
- 어디에 쓸지 애매하면 `PLAN.md` 에 쓴다. 여러 세션이 반복해서 필요로 한다고 드러날 때 위 문서로 올린다.
- 세 영구 문서는 같은 사실을 두 번 적지 않는다. 한쪽을 고치면 다른 쪽과 어긋나지 않는지 확인한다.
- 새 메타 문서는 만들지 않는다. 위 넷 중 어디에도 속하지 않는 내용이 쌓였을 때만 만든다.

## 디렉터리와 명령

| 경로 | 역할 |
|---|---|
| `content/week/` | 주간호 원본 마크다운. 사이트와 월간·연간 인사이트의 입력이다 |
| `scripts/` | 반복 실행되는 코드. 빌드와 기사 원문 추출 |
| `site/` | 빌드 산출물. 커밋하지 않으며 CI 가 배포한다 |
| `docs/` | 프로젝트 문서. 코드를 두지 않는다 |

```bash
npm ci && npm run build              # content/week/*.md → site/
node scripts/read-article.mjs <URL>  # 기사 본문과 발행일 추출
```
