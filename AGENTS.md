# AGENTS.md

**이 저장소를 고칠 때 지킬 규칙.** AI 에이전트가 세션마다 읽는 파일이므로 컨텍스트 비용이 들고, 그래서 짧게 유지한다.

## 이 저장소는 무엇인가

지난 한 주의 뉴스를 국내 5건·해외 5건·AI 5건으로 정리해 매주 월요일 오전(KST) GitHub Pages로 발행한다.

## 작업 규칙

- 확정 전에 실험한다. 되는지 확인하지 않은 것을 된다고 쓰지 않는다.
- 바꾸기 전에 논의한다. 선택지와 근거를 먼저 내놓고 결정은 사용자가 한다.
- 근거는 선례에서 찾는다. 자기 판단보다 기성 관행을 확인한 결과를 우선한다.
- 억지 부리지 않는다. 안 되는 건 안 된다고 말하고 한계를 그대로 밝힌다.
- 주간 발행 작업이면 [`docs/RUNBOOK_WEEKLY.md`](docs/RUNBOOK_WEEKLY.md) 를 처음부터 끝까지 읽고 그대로 따른다.
- 기준을 바꾸려 하면 [`docs/DECISIONS.md`](docs/DECISIONS.md) 를 먼저 읽는다. 이미 검토하고 버린 선택지가 적혀 있다.

## 문서 작성 규칙

- 가독성: 의미 단위의 subsection으로 나누고 포인트당 하나의 bullet을 쓴다.
- 간결성: 중복 표현을 피하고 하나의 의미는 하나의 문장으로 압축한다.
- 최소 변경: 내용을 추가할 때 기존 문서의 맥락을 고려해 최소한으로 바꾼다.
- 명확성 우선: 짧게 줄여서 뜻이 갈릴 수 있으면 길더라도 명확한 쪽을 쓴다. 기간·단위·날짜 표기가 특히 그렇다.
- 평이한 문체: 비유와 추상적 수식을 기본값으로 쓰지 않는다. 발행물의 문체 규칙은 [`docs/RUNBOOK_WEEKLY.md`](docs/RUNBOOK_WEEKLY.md) 에 있다.
- 도구 중립: 절차는 **도구 이름이 아니라 할 일**로 쓴다. "WebSearch 로 검색한다"가 아니라 "웹 검색으로 후보를 찾는다"로 쓴다.
- 플랫폼 분리: 특정 제품에서만 되는 것은 본문에 섞지 않고 [`docs/RUNBOOK_WEEKLY.md`](docs/RUNBOOK_WEEKLY.md) 의 "플랫폼별 실행" 절에 모은다.

## 문서의 역할 분담

| 문서 | 역할 |
|---|---|
| `AGENTS.md` | 저장소 작업 규칙 |
| `README.md` | 프로젝트 소개와 오류 신고 경로 |
| `docs/INTENT.md` | 목적과 품질 기준 |
| `docs/DECISIONS.md` | 결정과 폐기 기록 |
| `docs/RUNBOOK_WEEKLY.md` | 주간호 실행 절차 |
| `docs/QUALITY_CHECKS.md` | 점검 항목과 실행 방법 |
| `docs/PLAN.md` | 진행 상황 |

### 어디에 쓸지

- 문서 첫 줄에 그 문서가 담는 것을 한 줄로 쓴다. 다른 문서로 보내는 안내는 쓰지 않는다.
- 애매하면 [`docs/PLAN.md`](docs/PLAN.md) 에 쓴다. 여러 세션이 반복해서 필요로 한다고 드러날 때 위 문서로 올린다.
- 새 메타 문서는 만들지 않는다. 위 표 어디에도 속하지 않는 내용이 쌓였을 때만 만든다.

### 어떻게 유지할지

- 영구 문서는 짧게 유지하고 상황판인 `docs/PLAN.md` 만 길어지는 것을 허용한다.
- 영구 문서들은 같은 사실을 두 번 적지 않는다. 한쪽을 고치면 다른 쪽과 어긋나지 않는지 확인한다.
- `docs/INTENT.md` 는 변경 빈도가 낮다. 실행 절차를 다듬는 일로는 건드리지 않는다.
- `docs/DECISIONS.md` 는 지우지 않고 쌓기만 한다. 바뀐 결정은 기존 항목에 대체 표시를 남기고 새 항목을 더한다.
- 문서를 크게 고쳤으면 [`docs/QUALITY_CHECKS.md`](docs/QUALITY_CHECKS.md) 를 돌린다. 매번 돌리지는 않는다.

## 디렉터리와 명령

| 경로 | 역할 |
|---|---|
| `content/week/` | 주간호 원본 마크다운. 사이트와 월간·연간 인사이트의 입력이다 |
| `scripts/` | 반복 실행되는 코드. 빌드와 기사 원문 추출 |
| `site/` | 빌드 산출물. 커밋하지 않으며 CI 가 배포한다 |
| `docs/` | 프로젝트 문서. 코드를 두지 않는다 |

```bash
npm ci && npm run build              # content/week/*.md → site/
node scripts/read-article.mjs <URL>  # 기사 본문과 발행일 추출
```
