# AGENTS.md

이 저장소에서 작업하는 모든 세션이 알아야 할 규칙.

## 이 저장소는 무엇인가

지난 한 주의 뉴스를 국내 5건·해외 5건·AI 5건으로 정리해 매주 월요일 오전 8시(KST)에 GitHub Pages로 발행한다.

## 작업 규칙

- 바꾸기 전에 논의한다.
  - 선택지와 근거를 먼저 내놓고 결정은 사용자가 한다.
  - 문서와 프롬프트는 사용자가 명시적으로 정한 것만 고치며, 고칠 문구를 먼저 보여 주고 승인받는다.
  - 예외로 `PLAN.md` 만 에이전트가 알아서 관리한다.
- 확정 전에 실험한다.
  - 확인하지 않은 것을 된다고 쓰지 않는다.
- 필요한 것만 적는다.
  - 실패가 조용히 지나가거나 되돌리기 어려운 것만 절차로 남긴다. 겪은 일을 전부 적지 않는다.
- 근거는 선례에서 찾는다.
  - 자기 판단보다 기성 관행을 확인한 결과를 우선한다.

## 문서 작성 규칙

- **문장을 어떻게 쓸지.** 저장소 문서와 발행물에 똑같이 적용된다. 발행물에만 해당하는 문체 규칙은 런북이 따로 갖는다.
  - 가독성: 의미 단위의 소제목으로 나누고 포인트당 하나의 불릿을 쓴다.
  - 간결성: 중복 표현을 피하고 하나의 의미는 하나의 문장으로 압축한다.
  - 명확성: 짧게 줄여서 뜻이 갈릴 수 있으면 길더라도 명확한 쪽을 쓴다.
  - 평이한 문체: 비유와 추상적 수식을 기본값으로 쓰지 않는다. 한 문장에 주장 하나만 담고 접속으로 길게 잇지 않는다.
  - 긍정 서술: **무엇인지를 먼저 쓴다.** "A가 아니라 B다"의 형식을 가급적 피한다. 두 개념을 가르는 것이 규칙의 본체일 때와 한계를 밝힐 때만 대조를 쓴다.
- **무엇을 어떻게 적을지.** 이 저장소의 규약이다.
  - 최소 변경: 내용을 추가할 때 기존 문서의 맥락을 고려해 최소한으로 바꾼다.
  - 시각은 KST: 날짜와 시각은 **한국 표준시로 계산하고 한국 표준시로 쓴다.** 실행 환경이 UTC 라서 그대로 쓰면 하루가 어긋난다. 문서와 발행 일정, 대상 주 계산, 사람에게 하는 보고가 모두 그렇다. 발행물에 적는 사건 날짜만 예외로 현지 날짜를 쓴다. 크론처럼 UTC 로만 표현되는 값은 UTC 임을 밝히고 KST 를 함께 적는다.
  - 도구 중립: 절차는 **도구 이름이 아니라 할 일**로 쓴다. "WebSearch 로 검색한다"가 아니라 "웹 검색으로 후보를 찾는다"로 쓴다.
  - 플랫폼 분리: 특정 제품에서만 되는 것은 절차 본문에 섞지 않고 주간 런북의 플랫폼 절에 모은다.

## 문서의 역할 분담

| 문서 | 역할 | 주의할 것 |
|---|---|---|
| `AGENTS.md` | 저장소 작업 규칙 | 세션마다 읽는 비용이 든다. 짧게 유지하고 실행 절차를 들이지 않는다 |
| `README.md` | 프로젝트 소개 | 독자를 향한 유일한 문서다. 내부 규약을 쓰지 않는다 |
| `docs/INTENT.md` | 프로젝트 목적과 품질 기준 | 변경 빈도가 낮다. 실행 절차를 다듬는 일로는 건드리지 않는다 |
| `docs/DECISIONS.md` | 주요 결정사항 및 폐기 기록 | 기준을 바꾸기 전에 먼저 읽는다. 이미 검토하고 버린 선택지가 있다. 지우지 않고 쌓으며, 바뀐 결정은 옛 항목을 '버린 것'으로 옮겨 한 줄로 줄이고 새 항목을 더한다 |
| `docs/RUNBOOK_WEEKLY.md` | 주간호 실행 절차 | 주간 발행 작업은 이 문서를 처음부터 끝까지 읽고 그대로 따른다 |
| `docs/RUNBOOK_QUARTERLY.md` | 분기호 실행 절차 | 주간 런북에서 이어서 돈다 |
| `docs/RUNBOOK_YEARLY.md` | 연간호 실행 절차 | 분기 런북에서 이어서 돈다 |
| `docs/QUALITY_CHECKS.md` | 품질 점검 항목 | 문서를 크게 고친 뒤에 돌린다. 매번 돌리지는 않는다 |
| `docs/PLAN.md` | 진행 상황 | 길어지는 것이 허용되는 유일한 문서다. 끝난 항목은 지운다 |

### 내용을 어디에 쓸지

- **한 문서에만 쓴다.** 위 표에서 역할이 가장 좁게 맞는 문서를 고르고, 두 문서에 걸쳐 보이면 더 좁은 쪽에 쓴다. 한 문서가 한 역할만 담고 문서끼리 서로를 덜 참조하게 하는 것이 목적이다.
- 문서 첫 줄에 그 문서가 담는 것을 한 줄로 쓴다. 다른 문서로 보내는 안내는 쓰지 않는다.
- 애매하면 진행 상황 문서에 쓴다. 여러 세션이 반복해서 필요로 한다고 드러날 때 위 표의 문서로 올린다.
- 새 메타 문서는 만들지 않는다. 위 표 어디에도 속하지 않는 내용이 쌓였을 때만 만든다.

### 문서를 어떻게 유지할지

- 영구 문서는 짧게 유지한다. 길어져도 되는 문서는 위 표가 정한다.
- 영구 문서들은 같은 사실을 두 번 적지 않는다. 한쪽을 고치면 다른 쪽과 어긋나지 않는지 확인한다.

## 디렉터리와 명령

| 경로 | 역할 |
|---|---|
| `content/week/` | 주간호 원본 마크다운. 사이트와 분기호의 입력이다 |
| `content/quarter/` | 분기호 원본. 연간호의 입력이다 |
| `content/year/` | 연간호 원본 |
| `data/runs/` | 실행 기록. 호마다 비용·토큰·모델·시간. `/stats/` 의 입력이다 |
| `scripts/` | 반복 실행되는 코드. 빌드와 기사 원문 추출 |
| `site/` | 빌드 산출물. 커밋하지 않으며 CI 가 배포한다 |
| `docs/` | 프로젝트 문서. 코드를 두지 않는다 |

```bash
npm ci && npm run build              # content/week/*.md → site/
node scripts/read-article.mjs <URL>  # 기사 본문과 발행일 추출
```
